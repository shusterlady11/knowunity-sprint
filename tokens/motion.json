{
  "motion": {
    "duration": {
      "spinner": {
        "$type": "duration",
        "$value": {
          "value": 800,
          "unit": "ms"
        },
        "$description": "800ms for one full turn of a loading spinner. USE: rotating the loading icon in button and buttonIcon while state=Loading. DON'T: transitions or one-off state changes (this is for repeating loops only). PAIR: motion.easing.linear. RULE: under prefers-reduced-motion, stop the rotation but keep the icon visible and mark the control busy (aria-busy) so the status stays readable."
      },
      "toggle": {
        "$type": "duration",
        "$value": {
          "value": 200,
          "unit": "ms"
        },
        "$description": "200ms for a control's knob or thumb to slide to its other position. USE: the knob in inputModeToggle moving between voice and keyboard (48px of travel). DON'T: repeating loops (use motion.duration.spinner) or button press feedback. PAIR: motion.easing.standard. RULE: under prefers-reduced-motion, skip the slide and move the knob instantly; the active icon color still shows the mode."
      },
      "pulse": {
        "$type": "duration",
        "$value": {
          "value": 1200,
          "unit": "ms"
        },
        "$description": "1200ms for one full pulse of a waiting indicator, dim to bright and back. USE: loadingDots, while Knowie processes a spoken answer. DON'T: rotation (use motion.duration.spinner) or one-off state changes. PAIR: motion.easing.inOut, motion.duration.pulseStagger, motion.opacity.pulseDim. RULE: under prefers-reduced-motion, stop the pulse and show the dots at full brightness; mark the indicator as a status so the wait is still announced."
      },
      "pulseStagger": {
        "$type": "duration",
        "$value": {
          "value": 150,
          "unit": "ms"
        },
        "$description": "150ms between one dot starting its pulse and the next, so the pulse travels left to right. USE: loadingDots (dot 2 starts one step after dot 1, dot 3 two steps). DON'T: use as a general delay. PAIR: motion.duration.pulse."
      }
    },
    "easing": {
      "linear": {
        "$type": "cubicBezier",
        "$value": [
          0,
          0,
          1,
          1
        ],
        "$description": "Constant-speed curve. USE: continuous loops such as the loading spinner, where speed must not change during a turn. DON'T: entrances, exits or position changes, which need acceleration or deceleration. PAIR: motion.duration.spinner."
      },
      "standard": {
        "$type": "cubicBezier",
        "$value": [
          0.2,
          0,
          0,
          1
        ],
        "$description": "Fast start, long gentle stop. USE: position changes of a control that stays on screen, such as the inputModeToggle knob. DON'T: continuous loops (use motion.easing.linear). PAIR: motion.duration.toggle."
      },
      "inOut": {
        "$type": "cubicBezier",
        "$value": [
          0.4,
          0,
          0.6,
          1
        ],
        "$description": "Gentle start and gentle stop. USE: pulses that fade up and back down, such as loadingDots. DON'T: position changes (use motion.easing.standard) or constant-speed loops (use motion.easing.linear). PAIR: motion.duration.pulse."
      }
    },
    "opacity": {
      "pulseDim": {
        "$type": "number",
        "$value": 0.3,
        "$description": "Opacity of a pulsing dot at its dimmest (30%); it peaks at full opacity. USE: loadingDots. DON'T: use for disabled or inactive states (use the color tokens). PAIR: motion.duration.pulse."
      }
    }
  }
}
